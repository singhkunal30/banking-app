server:
  port: 8103
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/loan-service
    username: postgres
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect